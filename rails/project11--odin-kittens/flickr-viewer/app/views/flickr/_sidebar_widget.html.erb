<div class="flickr">
  <ul>
    <% photos.each do |row| -%>
      <li>
        <% row.each do |p| %>
          <%= link_to(image_tag(FlickRaw.url(p), :class => 'flickr_photo', :title => p.title, :border => 1), FlickRaw.url_profile(p)) %>
        <% end %>
      </li>
    <% end -%>
  </ul>
</div>